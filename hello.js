// Declare a variable
var sequence;

// Set the value of the variable
sequence = 1;

// Output the variable
console.log("Hello world" + sequence);

// Change the value of the variable
sequence = 2;

// Statements execute in order. So in the output below, of course we'll see 2, not 1.
console.log("Now I'm going to say " + sequence);
